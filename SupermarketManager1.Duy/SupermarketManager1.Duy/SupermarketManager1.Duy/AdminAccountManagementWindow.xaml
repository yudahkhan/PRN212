<Window x:Class="SupermarketManager1.Duy.AdminAccountManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SupermarketManager1.Duy"
        
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}"
        
        mc:Ignorable="d"
        Title="Account Management" Height="682" Width="1200" Loaded="Window_Loaded"
        WindowStartupLocation="CenterScreen">

    <materialDesign:Card Padding="20" Margin="10,10,10,50">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,20">
               
                <TextBlock Text="ACCOUNT MANAGEMENT" FontSize="24" FontWeight="Bold" 
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource PrimaryHueMidBrush}"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox x:Name="RoleFilterComboBox" Grid.Column="0"
                          materialDesign:HintAssist.Hint="Filter by Role"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Width="200" SelectionChanged="RoleFilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="All" Tag="0" IsSelected="True"/>
                    <ComboBoxItem Content="Admin" Tag="1"/>
                    <ComboBoxItem Content="Manager" Tag="2"/>
                    <ComboBoxItem Content="Staff" Tag="3"/>
                </ComboBox>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Click="CreateButton_Click" Width="120" Height="40" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Create"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignOutlinedButton}"
                            Click="UpdateButton_Click" Width="100" Height="40" Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Edit"/>
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource MaterialDesignRaisedButton}"
                            Click="DeleteButton_Click" Width="100" Height="40" 
                            Background="Red" BorderBrush="Red">
                        <StackPanel Orientation="Horizontal">
                            
                            <TextBlock Text="Delete"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>

            <DataGrid x:Name="AccountDataGrid" Grid.Row="2" 
                      Style="{StaticResource MaterialDesignDataGrid}"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      SelectionMode="Single"
                      GridLinesVisibility="None"
                      RowHeaderWidth="0"
                      AutoGenerateColumns="False" 
                      SelectionChanged="AccountDataGrid_SelectionChanged">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding AccountId}" Width="50"/>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="*"/>
                    <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                    <DataGridTextColumn Header="Phone" Binding="{Binding PhoneNumber}" Width="120"/>
                    <DataGridTextColumn Header="Role" Binding="{Binding Role.RoleName}" Width="100"/>
                    <DataGridTextColumn Header="Store" Binding="{Binding Warehouse.WarehouseName}" Width="*"/>

                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Status}" Value="Active">
                                        <Setter Property="Foreground" Value="Green"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding Status}" Value="Locked">
                                        <Setter Property="Foreground" Value="Red"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>

                    <DataGridTextColumn Header="Created Date" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Close" Width="100" Height="40" 
                        Style="{StaticResource MaterialDesignOutlinedButton}"
                        Click="CloseButton_Click"/>
            </StackPanel>
        </Grid>
    </materialDesign:Card>
</Window>