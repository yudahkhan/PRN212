<Window x:Class="SupermarketManager1.Duy.StaffManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Quản lý Nhân viên" Height="600" Width="1000" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Label Content="👥 QUẢN LÝ NHÂN VIÊN" Grid.Row="0" FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="➕ Tạo mới" Width="120" Height="40" Margin="0,0,10,0" 
                    Click="CreateButton_Click" Background="#4CAF50" Foreground="White" FontWeight="Bold"/>
            <Button Content="✏️ Sửa" Width="100" Height="40" Margin="0,0,10,0" 
                    Click="UpdateButton_Click" Background="#2196F3" Foreground="White" FontWeight="Bold"/>
            <Button Content="🗑️ Xóa" Width="100" Height="40" 
                    Click="DeleteButton_Click" Background="#F44336" Foreground="White" FontWeight="Bold"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="StaffDataGrid" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding AccountId}" Width="50"/>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                <DataGridTextColumn Header="Họ tên" Binding="{Binding FullName}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTextColumn Header="SĐT" Binding="{Binding PhoneNumber}" Width="120"/>
                <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Active">
                                    <Setter Property="Foreground" Value="Green"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Inactive">
                                    <Setter Property="Foreground" Value="Gray"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Locked">
                                    <Setter Property="Foreground" Value="Red"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Ngày tạo" Binding="{Binding CreatedAt, StringFormat={}{0:dd/MM/yyyy}}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>

