<Window x:Class="SupermarketManager1.Duy.ReportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Báo cáo Doanh thu" Height="700" Width="1200" Loaded="Window_Loaded">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Label Content="📊 BÁO CÁO DOANH THU" Grid.Row="0" FontSize="24" FontWeight="Bold" 
               HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Filters -->
        <GroupBox Header="Lọc báo cáo" Grid.Row="1" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Label Content="Kho:" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="WarehouseFilterComboBox" Grid.Column="1" Margin="0,0,20,0"
                          DisplayMemberPath="WarehouseName" SelectedValuePath="WarehouseId"
                          SelectionChanged="FilterComboBox_SelectionChanged">
                    <ComboBoxItem Content="Tất cả" Tag="0" IsSelected="True"/>
                </ComboBox>

                <Label Content="Từ ngày:" Grid.Column="2" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="FromDatePicker" Grid.Column="3" Margin="0,0,20,0"
                           SelectedDateChanged="DatePicker_SelectedDateChanged"/>

                <Label Content="Đến ngày:" Grid.Column="4" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <DatePicker x:Name="ToDatePicker" Grid.Column="5" Margin="0,0,0,0"
                           SelectedDateChanged="DatePicker_SelectedDateChanged"/>
            </Grid>
        </GroupBox>

        <!-- Summary -->
        <GroupBox Header="Tổng kết" Grid.Row="2" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <Label Content="Tổng doanh thu:" FontSize="16" FontWeight="Bold"/>
                    <Label x:Name="TotalRevenueLabel" Content="0 VNĐ" FontSize="18" FontWeight="Bold" 
                           Foreground="Red" Margin="10,0,0,0"/>
                    <Label Content="Tổng số đơn:" FontSize="16" FontWeight="Bold" Margin="30,0,0,0"/>
                    <Label x:Name="TotalOrdersLabel" Content="0" FontSize="18" FontWeight="Bold" 
                           Foreground="Blue" Margin="10,0,0,0"/>
                </StackPanel>

                <!-- DataGrid Sales -->
                <DataGrid x:Name="SalesDataGrid" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Ngày bán" Binding="{Binding SaleDate, StringFormat={}{0:dd/MM/yyyy HH:mm}}" Width="150"/>
                        <DataGridTextColumn Header="Kho" Binding="{Binding Warehouse.WarehouseName}" Width="*"/>
                        <DataGridTextColumn Header="Mã SP" Binding="{Binding ProductCode}" Width="100"/>
                        <DataGridTextColumn Header="Tên sản phẩm" Binding="{Binding ProductCodeNavigation.NameP}" Width="*"/>
                        <DataGridTextColumn Header="SL" Binding="{Binding QuantitySold}" Width="60"/>
                        <DataGridTextColumn Header="Đơn giá" Binding="{Binding UnitPrice, StringFormat={}{0:N0}}" Width="120"/>
                        <DataGridTextColumn Header="Thành tiền" Binding="{Binding TotalAmount, StringFormat={}{0:N0} VNĐ}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                    <Setter Property="Foreground" Value="Green"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Nhân viên" Binding="{Binding Account.FullName}" Width="*"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Đóng" Width="100" Height="40" 
                    Click="CloseButton_Click" Background="#9E9E9E" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>

